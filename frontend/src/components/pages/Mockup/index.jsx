import style from './index.module.sass'

const Mockup = () => (
  <section className={ style.container }>
    <h1>Станица не найдена</h1>
    <h2>Попоробуйте указать имя кандидата в URL</h2>
  </section>
)

export default Mockup
